import styled from "styled-components";

const Image = styled.img`
  height: 400px;
  margin: 1rem 0;
  @media (max-width: 600px) {
    height: 200px;
    margin: 1rem 0;
  }
`;

export default Image
